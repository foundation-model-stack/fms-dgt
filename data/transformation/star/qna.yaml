task_name: star_cot_transform
created_by: IBM
data_builder: star_transform
task_description: STaR algorithm for bootstrapping reasoning
data_formatter_templates:
  cot_transform:
    input: "{{input}}"
    output: "{{output}}"
include:
  tasks:
    - ../cot/qna.yaml
